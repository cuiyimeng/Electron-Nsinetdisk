const state = {
  appMode: 'overlap'
}

const mutations = {

}

const getters = {

}

const actions = {
  test ({ commit, dispatch, state, rootState }) {
  }
}

export default {
  state,
  mutations,
  actions,
  getters
}
